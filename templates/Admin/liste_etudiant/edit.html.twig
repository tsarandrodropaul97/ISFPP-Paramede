{% extends 'Admin/base.html.twig' %}

{% block title %}Edit Partenaires
{% endblock %}

{% block body %}
	{{ include("Admin/partials/titre.html.twig", {'titre':'Editer un unité d\'enseignement'} ) }}
	<br>
	<div class="row">
		<div class="col-12">
			<div class="card">
				<div class="card-body">
					{{ form_start(form)}}
					<div class="row">
						<div class="col-12 bg-bprimary">
							{{ form_row(form.nom)}}
						</div>
						<div class="col-12">
							{{ form_row(form.code)}}
						</div>
					</div><br>
					<button type="submit" class="btn btn-success"><i class="fas fa-edit"></i> Enregistrer</button>
					{{ form_end(form)}}
				</div><br>
			</div>
		</div>
	</div>
{% endblock %}
